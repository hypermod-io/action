{
  "name": "@hypermod/action",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --transpile-only --minify"
  },
  "engines": {
    "node": ">= 18"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.1.1",
    "@octokit/plugin-throttling": "^5.2.1",
    "fs-extra": "^8.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
